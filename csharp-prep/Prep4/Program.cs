using System;

class Program
{
    static void Main(string[] args)
    {
        //Console.WriteLine("Hello Prep4 World!");
        // Asks for numbers untill the user enters a zero and adds them to the list

        int user_input = -1;

        do {
        Console.Write("enter numbers in whole intigers only: ");
        
        } while (user_input != 0);
    }
}